@page "/"
@inject ClientModel ClientModel

<HeaderRow Header="RestlessDb: UI for the Restless Db .net Core Api Service" 
           SubHeader="Execute and configure queries and display the result" 
           ErrorMessage=@ClientModel.ErrorMessage/>

@code {
    protected override async Task OnInitializedAsync()
    {
        await ClientModel.CheckInitAsync();
    }
    
}
